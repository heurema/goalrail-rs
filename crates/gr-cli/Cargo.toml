[package]
name = "gr"
version.workspace = true
edition = "2024"
license = "MIT"

[dependencies]
clap = { version = "4", features = ["derive"] }
gr-inspect-codex = { version = "0.3.3", path = "../gr-inspect-codex" }

[dev-dependencies]
serde_json = "1"
