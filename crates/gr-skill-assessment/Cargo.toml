[package]
name = "gr-skill-assessment"
version = "0.2.0"
edition = "2024"
license = "MIT"
publish = false

[dependencies]
serde = { version = "1", default-features = false, features = ["alloc", "derive"] }
